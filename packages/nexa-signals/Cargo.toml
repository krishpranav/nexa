[package]
name = "nexa-signals"
version = "0.1.0"
edition = "2024"
authors = ["Nexa Team"]
license = "MIT"

[dependencies]
slotmap = "1.0"
smallvec = "1.0"
# nexa-core = { path = "../nexa-core", version = "0.1.0" } # Maybe needed later for integration
