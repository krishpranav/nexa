[package]
name = "nexa-renderer-gpu"
version = "0.1.0"
edition = "2024"
authors = ["Nexa Team"]
license = "MIT"

[dependencies]
wgpu = "0.20"
winit = "0.30"
futures = "0.3"
tracing = "0.1"
thiserror = "1.0"
# nexa-core = { path = "../nexa-core", version = "0.1.0" } # If needed for scene conversion

[features]
default = []
