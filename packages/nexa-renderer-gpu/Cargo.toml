[package]
name = "nexa-renderer-gpu"
version = "0.1.0"
edition = "2024"
authors = ["Nexa Team"]
license = "MIT"

[dependencies]
wgpu = "22.0" # Latest stable or compatible
winit = { version = "0.29", features = ["rwh_05"] }
futures = "0.3"
tracing = "0.1"
thiserror = "1.0"
bytemuck = { version = "1.14", features = ["derive"] }
glam = "0.24"

[features]
default = []
