[package]
name = "nexa-rsx"
version = "0.1.0"
edition = "2024"
authors = ["Nexa Team"]
license = "MIT"

[lib]
proc-macro = true

[dependencies]
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
proc-macro2 = "1.0"
# nexa-core dependency removed to avoid circular dependency (rsx macro generates code that refers to nexa-core, but doesn't link to it)
