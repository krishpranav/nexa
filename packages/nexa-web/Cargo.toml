[package]
name = "nexa-web"
version = "0.1.0"
edition = "2024"
authors = ["Nexa Team"]
license = "MIT"

[dependencies]
nexa-core = { path = "../nexa-core", version = "0.1.0" }
web-sys = { version = "0.3", features = ["Window", "Document", "Element", "Node", "console"] }
wasm-bindgen = "0.2"
js-sys = "0.3"

[features]
default = []
